fun main(args: Array<String>) {
    var x:Int
    val y:Int
    x = 20
    x = 30
    println(x)

    y = 40
    println(y)

    var principal = 10000
    var rate = 2.5
    var time = 5
    var interest = (principal * rate * time)/100.0
    println("Your interest is $interest")

    // Calculating the volume of a cylinder
    var height = 30
    var pie = 3.142
    var diameter = 45
    var volume = (pie * diameter * height)
    println("your volume is $volume ")
}
